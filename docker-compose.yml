services:
  proxy:
    image: nginx:1.27-alpine
    depends_on:
      web:
        condition: service_healthy
      server:
        condition: service_healthy
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - ./deploy/nginx/edge.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - world-at-war
    restart: unless-stopped

  server:
    image: world-at-war-server:production
    build:
      context: .
      dockerfile: crates/server/Dockerfile
      additional_contexts:
        c3mesh: ../c3mesh
    environment:
      BIND_ADDR: 0.0.0.0:8000
      ADMIN_SETUP_TOKEN: ${ADMIN_SETUP_TOKEN:-}
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
      SPACETRACK_USERNAME: ${SPACETRACK_USERNAME:-}
      SPACETRACK_PASSWORD: ${SPACETRACK_PASSWORD:-}
      SPACE_CARDS_DIR: /app/data/generated/space-cards
      AIRPORT_CACHE_DIR: /app/data/cache/airports
      AIRPORT_REFRESH_MAX_AGE_SECONDS: ${AIRPORT_REFRESH_MAX_AGE_SECONDS:-86400}
      FAA_NASR_APT_URL: ${FAA_NASR_APT_URL:-}
    user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
    volumes:
      - ./data/cache/space-track:/app/data/cache/space-track
      - ./data/cache/airports:/app/data/cache/airports
      - ./data/generated/space-cards:/app/data/generated/space-cards:ro
    expose:
      - "8000"
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent http://localhost:8000/health >/dev/null"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s
    networks:
      - world-at-war
    restart: unless-stopped

  web:
    image: world-at-war-web:production
    build:
      context: .
      dockerfile: web/Dockerfile
    expose:
      - "80"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    networks:
      - world-at-war
    restart: unless-stopped

  e2e:
    profiles: ["test"]
    image: world-at-war-e2e:test
    build:
      context: .
      dockerfile: web/Dockerfile.e2e
      additional_contexts:
        c3mesh: ../c3mesh
    init: true
    ipc: host

networks:
  world-at-war:
    driver: bridge
